import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#e0e0e0',
        margin: 15,
        borderRadius: 5,
        flexDirection: 'row',
        alignItems: 'center',
        padding: 5,
    },
    textInput: {
        flex: 1,
    },
});