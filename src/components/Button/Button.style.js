import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#64b5f6',
        margin: 15,
        borderRadius: 5,
        padding: 10,
        alignItems: 'center',
    },
    text: {
        fontWeight: 'bold',
        fontSize: 16,
        color: '#FFFFFF',
    },
});